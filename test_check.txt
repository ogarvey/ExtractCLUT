string[] testFiles = {
    "coded_packed_8bpp.cel",
    "coded_unpacked_8bpp.cel",
    "coded_unpacked_16bpp.cel",
    "uncoded_packed_8bpp.cel"
};
foreach (var file in testFiles) {
    var cel = CelUnpacker.UnpackCelFile("3do/examples/" + file, false, 0);
    Console.WriteLine($"{file}: HasTransparency={cel?.TransparencyMask != null}, HasAMV={cel?.AlternateMultiplyValues != null}, BPP={cel?.BitsPerPixel}");
}
